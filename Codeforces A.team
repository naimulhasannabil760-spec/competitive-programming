#include<iostream>
using namespace std;
int main()
{
	int tc;
	cin >> tc;
	int count = 0;
	while(tc--)
	{
		int a,b,c;
		cin >> a >> b >> c;
		if(a+b+c>=2)
		{
			count++;
		}
	}
	cout << count << endl;
	return 0;
}
